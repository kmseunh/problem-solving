class Solution {
    public String solution(String[] arr) {
        StringBuilder sb = new StringBuilder();
        
        for (String word : arr) {
            sb.append(word);
        }
        
        return sb.toString();
    }
}